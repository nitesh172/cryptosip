const USER_NOT_FOUND = "User does not exist."
const EMAIL_ALREADY_EXIST = "Email already exists."
const EMAIL_ALREADY_USE = "Email already in use."
const INTERNAL_ERROR = "Default internal error."
const INVALID_CREDENTIAL = "Incorrect credential."
const INVALID_EMAIL_FORMAT = "Incorrect email format."
const INVALID_PASSWORD_FORMAT = "Incorrect password format."
const DEFAULT_MESSAGE = "General fallback message."
const TOO_MANY_ATTEMPTS_TRY_LATER = "Too many attempt."
const PASSWORD_WRONG = "You entered wrong password."
const USER_DISABLED = "User temporary disabled."
const PASSWORD_WEAK = "Weak password."
const INVALID_API_KEY = "Invalid API key"
const INVALID_ACTION_CODE = "Link expired."

export const FirebaseError = {
    USER_NOT_FOUND,
    EMAIL_ALREADY_EXIST,
    INTERNAL_ERROR,
    INVALID_CREDENTIAL,
    DEFAULT_MESSAGE,
    INVALID_EMAIL_FORMAT,
    PASSWORD_WRONG,
    TOO_MANY_ATTEMPTS_TRY_LATER,
    INVALID_PASSWORD_FORMAT,
    USER_DISABLED,
    PASSWORD_WEAK,
    INVALID_API_KEY,
    INVALID_ACTION_CODE,
    EMAIL_ALREADY_USE
}