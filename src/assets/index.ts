const Logo = require("./images/logo.png")
const Logo2 = require("./images/logow.png")
const LogoMini = require("./images/logomini.png")
const data = require('./data')
const notification = require('./images/notification.svg').default
const under_construction = require('./images/under_construction.svg').default

const images = {
  Logo,
  Logo2,
  LogoMini,
  notification,
  under_construction
}

export { images, data }
